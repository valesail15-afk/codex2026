services:
  afk-app:
    build: .
    container_name: afk-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3001:3001"
    volumes:
      - /opt/afk/data/arbitrage.db:/app/arbitrage.db
